{
  "private": true,
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "preinstall": "pnpm only-allow pnpm",
    "dev": "turbo watch transit:packages dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "typegen": "turbo run typegen",
    "lint:local": "oxlint --fix",
    "lint": "oxlint --format=github",
    "format:local": "biome check --write",
    "format": "biome check --reporter=github"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "only-allow": "catalog:",
    "oxlint": "catalog:",
    "tsx": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": "22.20.0",
    "pnpm": "10.15.0"
  }
}
